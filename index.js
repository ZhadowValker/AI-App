// index.js — Expo entry point
// Required when using bare workflow or custom entry.
// registerRootComponent ensures App renders correctly
// inside Expo Go and in standalone builds.

import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
